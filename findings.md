# 调研与设计结论

## 环境

- 当前工作区没有已有项目文件或 Git 仓库，属于从零开始的 Android 项目。
- 用户没有实体手机，先使用普通 Android 模拟器开发和验证。
- 目标兼容设备为华为 Mate 30 Pro，系统为 HarmonyOS 5.0。
- 用户确认该设备可以安装运行 APK，也可以运行大麦 App。

## 产品边界

- 目标是个人单账号、单台设备、单个任务的预约售票辅助工具。
- 交付形式为可旁加载安装的 APK。
- 首版只覆盖一条固定流程：预约 -> 等待开售 -> 选择票档、数量和观演人 -> 人工处理人机校验 -> 提交订单 -> 进入付款页面停止。
- 不自动输入支付密码、短信验证码，不绕过验证码、滑块、排队或风控。

## 观演人策略

- 用户先在大麦官方 App 中维护观演人。
- 任务配置中选择与票数完全匹配的具体观演人。
- 例如 4 个观演人买 2 张票，必须提前选定 2 人；不按页面顺序猜选。
- 找不到指定人员、姓名重复且无法确认或数量不匹配时暂停。

## 票档策略

- 在大麦当前可见页面读取票档名称、价格和可选状态。
- 任务保存票档优先级，例如主选票档和备用票档。
- 开售时重新读取当前页面，按“名称 + 价格 + 可选状态”匹配。
- 预约阶段看不到票档时允许用户手动配置；开售时再重新读取。
- 匹配不明确时不盲目点击，转人工处理。

## 页面读取原理

- 使用 `AccessibilityService` 监听窗口和内容变化。
- 遍历 `AccessibilityNodeInfo` 的文本、描述、可点击状态、启用状态、边界和父子关系。
- 按票档容器组合名称、价格和状态。
- 仅在无障碍树无法暴露必要信息时考虑本地 OCR 作为备用；不读取隐藏接口数据。

## 主要风险

- HarmonyOS 5 的权限、后台保活和无障碍行为需在真实华为设备验证。
- 大麦页面改版、WebView/自绘控件或动态票档可能导致页面识别失效。
- 模拟器不能代表真实设备，也可能被目标 App 识别为模拟环境。
- 自动化操作可能受大麦服务条款限制，产品必须保持人工处理校验和支付的边界。
