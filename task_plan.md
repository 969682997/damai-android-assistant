# 大麦 Android 抢票助手基础版

## 目标

在普通 Android 模拟器上先开发并验证一个可安装 APK 的基础版，随后用华为 Mate 30 Pro + HarmonyOS 5 做兼容性验证。工具仅操作大麦官方 App 的可见界面，自动完成预约、开售后的固定选票流程，遇到人机校验交给用户，并在付款页面停止。

## 阶段

| 阶段 | 状态 | 说明 |
|---|---|---|
| 1. 需求与边界确认 | 已完成 | 单账号、单设备、单任务；APK 旁加载；人工处理验证码和支付 |
| 2. 基础设计文档 | 已完成 | 已记录架构、页面读取、观演人、票档、权限和测试策略 |
| 3. 用户审阅设计文档 | 已完成 | 用户确认继续，设计文档作为实施依据 |
| 4. 实施计划 | 已完成 | 已按 TDD 拆分 Android 工程、领域逻辑、无障碍服务和测试任务 |
| 5. APK 基础实现 | 进行中 | 领域层、页面读取、状态机、权限和基础界面已实现，待完整构建验证 |
| 6. 模拟器验证 | 待开始 | 验证权限、任务、页面识别和人工接管 |
| 7. 华为兼容性验证 | 待开始 | 需要 Mate 30 Pro + HarmonyOS 5 或可信远程真机 |

## 当前决策

- 第一版目标设备为华为 Mate 30 Pro + HarmonyOS 5。
- 用户确认 HarmonyOS 5.0 可以安装运行 APK，也可以运行大麦 App；仍需实测无障碍控制能力。
- 第一版先使用普通 Android 模拟器开发和测试。
- 采用 Android 无障碍服务读取可见控件和执行正常点击。
- 票档通过大麦当前可见页面读取，任务中支持固定票档和备用票档优先级。
- 观演人由用户提前在大麦官方 App 中维护，助手只勾选用户预设的具体人员。
- 遇到验证码、滑块、短信验证、页面歧义或异常时暂停并提示人工接管。
- 进入付款页面后强制停止自动操作；不读取或保存支付密码和短信验证码。
- 不调用大麦未公开接口，不绕过验证码、排队或设备风控。
- 第一版不做远程控制、多账号、多设备、服务器和模拟器批量管理。
- 实施计划已保存到 `docs/superpowers/plans/2026-07-28-damai-android-assistant-plan.md`，并完成占位符、文件结构和测试隔离自检。

## 当前实现摘要

- 已完成任务存储和脱敏日志，使用应用私有 `SharedPreferences`，任务数据为版本化 JSON。
- 已完成票档匹配和观演人选择，选择不明确时转人工，不随机勾选。
- 已完成无障碍节点快照和页面读取，仅读取大麦可见节点。
- 已完成付款页终止和人机校验人工接管边界。
- 已完成 APK 基础界面和权限门禁；缺少权限时“开始任务”按钮不可用。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| 读取规划技能时使用了错误的代理目录路径 | 1 | 改用 `C:\\Users\\96968\\.codex\\skills\\planning-with-files\\SKILL.md`，读取成功 |
